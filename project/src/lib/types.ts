export type Restaurant = {
    name: string,
    picture: string,
    category: string,
    price: number,
    open: boolean
}

export type Review = {
    name: string,
    image: string,
    text: string,
    rating: number
}